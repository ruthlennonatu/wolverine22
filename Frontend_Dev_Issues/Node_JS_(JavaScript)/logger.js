
//adding winston logging library for security analysis
//This file has some dependencies for security analysis
//Software bill of material will be generated and a suitable tool

'use strict';

/*
const winston = require('winston');
const tsFormat = () => (new Date()).toLocaleTimeString();

let logger = winston.createLogger({ 
  transports: [
    // colorize the output to the console
    new (winston.transports.Console)({
      timestamp: tsFormat,
      colorize: true,
      level: 'info'
    }),
  ],
  exitOnError: false
});

module.exports = logger;
*/